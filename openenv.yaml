name: adaptive-study-env

endpoints:
  reset: /reset
  step: /step
  state: /state

tasks:
  - id: learning
    grader:
      type: info
      key: score

  - id: stress
    grader:
      type: info
      key: score

  - id: focus
    grader:
      type: info
      key: score
