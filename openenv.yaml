name: adaptive-study-env

endpoints:
  reset: /reset
  step: /step
  state: /state

tasks:
  - id: task1
    grader:
      type: reward
      normalize: true

  - id: task2
    grader:
      type: reward
      normalize: true

  - id: task3
    grader:
      type: reward
      normalize: true
