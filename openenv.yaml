name: adaptive-study-env

endpoints:
  reset: /reset
  step: /step
  state: /state

tasks:
  - id: t1
    grader:
      type: reward
      normalize: false

  - id: t2
    grader:
      type: reward
      normalize: false

  - id: t3
    grader:
      type: reward
      normalize: false
